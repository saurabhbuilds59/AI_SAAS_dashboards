# Base URL the frontend uses for API calls.
# In dev this is proxied by Vite (see vite.config.js) so /api works as-is.
# In production, point this at your deployed backend, e.g. https://api.yourapp.com/api
VITE_API_BASE_URL=/api
